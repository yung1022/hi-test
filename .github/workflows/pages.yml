name: Deploy control panel

on:
  push:
    branches: [main]
    paths:
      - "web/**"
      - "overlay/**"
      - "config/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Stage site
        run: |
          mkdir -p _site
          cp -R web overlay config _site/
          cat > _site/index.html <<'HTML'
          <!DOCTYPE html>
          <meta charset="utf-8" />
          <meta http-equiv="refresh" content="0; url=./web/" />
          <link rel="canonical" href="./web/" />
          <title>LIVE DESK</title>
          <a href="./web/">Open LIVE DESK control panel</a>
          HTML

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v4
